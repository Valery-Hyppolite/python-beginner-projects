The calculator tip

bill = input("enter the total bill ")
tip = input("how much tips you want to give? ")
people = input("how many people will be spliting this bill? ")
percent = (int(bill))/(int(people)) *(int(tip))/100
person_bill= (round(percent,2))
print(f"each person should pay ${person_bill}.")
